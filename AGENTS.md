# AGENTS.md — 模型约束指令

包含模型执行中反复纠结和出错的点，作为硬性约束。

---

## 摧毁性操作限制

- **任何 `rm`、`git rm`、删除文件/目录的操作，必须先 `git commit` 当前所有非临时的变更，再经用户明确确认后方可执行。**
- **单次 `rm` 的目标对象不得超过 5 个。** 超过 5 个需分批执行，每批之间等待用户确认。
- 不得将 `rm` 命令与其他命令链式连接执行。
- 删除前必须列出将要删除的文件清单，让用户知晓。

## 构建验证

- 重大重构后必须运行 `npx vitepress build` 验证。
- 构建失败时，分析错误输出，修复后重新构建，不要跳过验证步骤。

## Git 操作

- 提交前确保 `git status` 已检查，不提交临时文件、构建产物。
- 每次逻辑阶段完成后提交，不要积攒大量变更一次性提交。

## 项目约定

- 组件命名不得出现 `Minecraft` 等特定名称。
- 所有文档内容使用标准 `.md` 文件，不要将内容写入 `.ts` 字符串。
- 开发笔记写入 `dev-notes/`，该目录被 VitePress 构建排除。

## 交互约定

- 执行多步骤任务前，先创建 todo list 并展示给用户。
- 每完成一个 todo 立即标记为 completed，不要批量标记。
- 遇到不确定的设计决策时，先询问用户，不要自行假设。